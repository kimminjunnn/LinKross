# LinKross 프로젝트 지침

이 문서는 저장소에서 작업하는 에이전트가 따라야 할 공통 지침이다. `AGENTS.md`와 `CLAUDE.md`의 공통 내용은 함께 유지한다. 한쪽의 제품 원칙이나 개발 규칙을 변경하면 다른 쪽도 같은 의미로 갱신한다.

## 1. 제품 정의

LinKross는 사내 CTO, 개발 리더 또는 전문 QA가 부족한 초기 스타트업이 외주 개발자를 선택하고, 업무 범위를 합의하고, 제출된 결과물이 실제로 작동하는지 검수하도록 돕는 B2B 프로젝트 검증 워크스페이스다.

핵심 메시지는 다음과 같다.

> 사람을 고르고, 일을 합의하고, 결과물을 검증한다.

제품의 세 가지 핵심 질문은 다음과 같다.

1. 누구에게 맡길 것인가?
2. 무엇을 만들 것인가?
3. 약속대로 작동하는가?

결제 상태와 통합 증빙은 이 흐름을 마무리하는 보조 기능이다. LinKross를 범용 프리랜서 마켓플레이스, 범용 QA 도구, 에스크로 또는 자동 지급 서비스로 확장 해석하지 않는다.

## 2. 사용자와 설계 관점

### 핵심 사용자

- 직원 약 5-30명의 초기 스타트업 또는 예비 창업팀
- 사내 개발 책임자 없이 웹 기반 MVP를 외주 발주하는 대표, PO, 운영 담당자
- GitHub 저장소를 보유하거나 최소한의 저장소 접근 권한을 가진 발주자

### 보조 사용자

- 국내외 프리랜서 개발자
- 소규모 개발 에이전시 담당자

### 비타깃

- 자체 개발팀과 체계적인 CI/CD 및 QA를 이미 운영하는 조직
- GitHub로 관리되지 않는 디자인, 번역, 영상 등 비개발 결과물 프로젝트
- 운영망, 규제 데이터, 복잡한 인프라까지 자동 검증해야 하는 고위험 엔터프라이즈 프로젝트

모든 화면과 설명은 비개발자가 이해하고 의사결정할 수 있는 언어를 우선한다. 코드, 빌드 로그, 기술 용어보다 다음 정보를 먼저 보여준다.

- 무엇을 약속했는가
- 무엇이 통과하거나 실패했는가
- 사용자가 직접 무엇을 확인해야 하는가
- 어떤 근거로 승인 또는 수정 요청을 할 수 있는가

## 3. 핵심 제품 흐름

기능을 서로 분리된 도구로 만들지 않는다. 앞 단계의 결과가 다음 단계의 입력이 되어야 한다.

1. 발주자가 요구사항, 제약, 제한시간과 평가 기준을 등록한다.
2. 지원자가 동일한 조건에서 질문, 요구 이해 요약, 실행 계획, 일정, 리스크와 가정을 제출한다.
3. 발주자가 원문과 루브릭 근거를 비교해 개발자를 선정한다.
4. 선정자의 응답과 원본 요구사항을 바탕으로 SOW, WBS, 마일스톤과 완료조건 초안을 만든다.
5. 발주자와 개발자가 동일 버전을 검토하고 각각 승인한다.
6. 개발자가 GitHub PR을 연결하고 특정 Commit SHA로 검수를 요청한다.
7. 일회성 격리 환경에서 설치, 빌드, 실행과 테스트를 수행한다.
8. 완료조건별 결과, 증거, 로그와 Preview를 제공한다.
9. 발주자가 승인하거나 수정 요청하고, 새 커밋은 같은 조건으로 재검수한다.
10. 승인 결과에 인보이스와 지급 상태를 연결하고 통합 증빙을 생성한다.

요구사항 문맥, 승인 버전, Commit SHA, 검수 결과와 사람의 결정이 끊기지 않게 연결되어야 한다.

## 4. MVP 우선순위

다음 순서를 지킨다.

1. 인재 역량검증
2. AI 기반 SOW 및 WBS 작성과 양측 승인
3. GitHub 실행형 마일스톤 검수
4. 결제 상태 및 통합 증빙

대표 데모를 끝까지 연결하는 작업을 부분 기능의 완성도보다 우선한다. 5주 MVP의 기준 시나리오는 Next.js 및 Node.js 기반 로그인 기능이다.

- 세 명의 지원자가 같은 요구사항에 질문, 계획과 리스크를 제출한다.
- 한 명을 선택해 SOW와 네 개의 완료조건을 승인한다.
- 개발자가 PR과 커밋을 제출한다.
- 별도 환경에서 로그인 기능을 실행하고 테스트한다.
- 실패 항목을 수정한 새 커밋을 동일 기준으로 재검수한다.
- 네 조건이 모두 충족되면 사람이 최종 승인한다.

핵심 로그인 완료조건 예시는 다음과 같다.

- 이메일과 비밀번호를 입력할 수 있다.
- 정상 로그인 후 `/dashboard`로 이동한다.
- 잘못된 비밀번호 입력 시 오류가 표시된다.
- 이메일 미입력 시 로그인이 차단된다.

## 5. 기능 요구사항

### 사용자와 프로젝트

- 발주자와 지원자/개발자의 역할과 접근 권한을 구분한다.
- 요구사항, 일정, 예산, 참고자료로 프로젝트를 생성한다.
- MVP는 프로젝트당 단일 외주 개발자와 단일 저장소를 우선 지원한다.

### 인재 역량검증

- 동일한 요구사항 문서, 제출 템플릿, 제한시간과 평가 기준을 모든 지원자에게 적용한다.
- 질문, 요구 이해 요약, 실행 계획, 일정, 리스크, 가정과 필요한 지원을 저장하고 제출할 수 있어야 한다.
- 제출 원문을 보존하며, AI 요약만으로 후보를 평가하지 않는다.
- 비교 화면에는 원문, 항목별 근거, 강점, 누락과 후속 질문을 함께 제공한다.
- 기본 평가 루브릭은 요구사항 이해 25, 질문의 질 20, 실행 계획 25, 리스크 인식 20, 커뮤니케이션 10이다.
- 이 기능은 코딩 실력의 완전한 증명이 아니라 요구 이해와 협업 역량을 비교하는 보조 검증이다.

### SOW, WBS와 완료조건

- SOW에는 목표, 범위, 제외사항, 결과물, 일정, 금액, Acceptance Criteria와 Definition of Done을 포함한다.
- 모호한 표현, 누락된 예외 조건과 상충하는 일정 또는 범위를 질문으로 드러낸다.
- 완료조건은 "잘 구현한다" 같은 표현 대신 사용자 행동과 예상 결과가 관찰 가능하도록 작성한다.
- 발주자와 개발자는 같은 문서 버전을 각각 승인해야 한다.
- 승인된 버전은 고정하고, 이후 변경은 새 버전과 변경 이력으로 관리한다.
- 승인된 완료조건을 GitHub 검수 체크리스트와 테스트의 기준으로 사용한다.

### GitHub 실행형 검수

- 저장소, PR과 Commit SHA를 마일스톤에 연결한다.
- 검수 대상은 변경 가능한 브랜치명이 아니라 불변의 Commit SHA로 고정한다.
- 설치, 빌드, 테스트 DB와 합성 데이터 구성, 서버 실행, 자동 테스트를 별도 환경에서 수행한다.
- MVP 자동 검수는 Playwright 기반 핵심 사용자 흐름 2-5개를 우선한다.
- 완료조건별로 `대기`, `검수 중`, `통과`, `실패`, `확인 필요`를 구분하고 근거를 제공한다.
- 스크린샷, 영상, 오류 로그와 Preview URL을 결과에 연결한다.
- 자동 판정이 어려운 디자인이나 문구는 `확인 필요`로 남겨 사람이 Preview에서 확인하게 한다.
- 새 커밋을 동일한 완료조건으로 재검수하고 이전 결과와 수정 이력을 보존한다.
- 테스트 결과만으로 승인이나 지급을 자동 실행하지 않는다. 최종 승인은 발주자가 한다.

### 결제 상태와 통합 증빙

- 마일스톤 금액, 지급 조건, 인보이스를 승인 결과에 연결한다.
- 지급 상태는 최소한 `지급 요청`, `처리 중`, `지급 완료`를 표현한다.
- 실제 송금은 외부 파트너나 사용자의 기존 방식으로 처리하고 LinKross는 상태와 참조값을 기록한다.
- 통합 증빙에는 요구사항 버전, 지원자 제출, 평가 근거, 승인된 SOW/WBS, 완료조건, 양측 승인, PR, Commit SHA, 검수 증거, 재검수 이력, 최종 승인, 인보이스와 지급 기록을 포함한다.
- 통합 증빙은 프로젝트 진행 확인 자료이며 법률, 세무 판단, 정식 전자계약을 대체한다고 표현하지 않는다.

## 6. 데이터와 이력 원칙

도메인 모델을 설계할 때 최소한 다음 개념을 명시적으로 분리한다.

- 사용자, 조직, 역할과 권한
- 프로젝트와 요구사항 버전
- 역량검증 과제, 지원자 응답과 평가 근거
- SOW 버전, WBS, 마일스톤과 완료조건
- GitHub 저장소, PR과 Commit SHA
- 검수 실행, 조건별 결과와 증거
- 수정 요청, 재검수와 최종 승인
- 인보이스, 지급 상태와 통합 증빙
- 감사 가능한 변경 및 승인 이벤트

중요 상태 변경은 행위자, 시각, 대상 버전과 변경 내용을 추적할 수 있어야 한다. 승인되거나 제출된 원문을 조용히 덮어쓰지 않는다. 사용자에게 보여주는 현재 상태와 감사용 이력을 모두 보존한다.

## 7. AI 사용 원칙

- AI는 평가 근거 구조화, 누락 탐지, 후속 질문, SOW/WBS, 완료조건과 테스트 초안을 보조한다.
- AI가 지원자 선정, 계약 조건, 일정, 비용, 법률 문구, 기능 승인 또는 지급 결정을 자동 확정하지 않는다.
- AI 결과에는 가능한 한 원문 근거와 불확실성을 함께 보여준다.
- 사용자가 초안을 편집하고 승인할 수 있어야 한다.
- 지원자에게 AI 사용 정책을 명확히 알리고 필요한 경우 AI 사용 여부를 기록한다.
- 테스트 실패가 제품 결함인지 테스트 시나리오 오류인지 이의를 제기하고 재실행할 수 있어야 한다.

## 8. 보안 원칙

검수 대상 코드는 신뢰하지 않는 코드로 취급한다.

- 운영 서버와 분리된 일회성 VM 또는 컨테이너에서 실행한다.
- GitHub App은 선택 저장소에 필요한 최소 권한만 요청한다.
- Runner에 운영 DB, 실제 고객 데이터, 장기 API 키, 클라우드 관리자 권한 또는 GitHub 쓰기 토큰을 제공하지 않는다.
- 합성 테스트 데이터만 사용하고 실제 고객 데이터와 동등하다고 표현하지 않는다.
- CPU, 메모리, 실행시간과 네트워크 접근을 제한한다.
- 검수 종료 후 코드와 실행환경을 폐기한다.
- 로그, 스크린샷과 영상에서 비밀정보를 마스킹한다.
- 증거와 민감정보의 보관기간 및 접근 권한을 설정한다.

보안 경계를 약화하는 구현은 편의상 도입하지 않는다. 외부 명령, 저장소 코드와 로그는 모두 잠재적으로 악의적인 입력으로 다룬다.

## 9. 범위 제한

MVP에 포함하지 않는다.

- 자체 프리랜서 풀을 운영하는 범용 구인 마켓플레이스
- 자금 보관, 에스크로, 실제 법정화폐 또는 스테이블코인 송금
- 전자계약, 법률·세무 판단, 분쟁 중재, KYC/KYB/AML의 직접 수행
- 모든 언어, 프레임워크와 인프라의 자동 지원
- 전체 코드 품질, 보안성과 확장성의 자동 판정
- 운영 고객 데이터 반출 또는 운영 서버에서의 무승인 테스트
- 다수 개발자와 다수 벤더가 동시에 참여하는 복합 프로그램 관리

범위를 넓히는 요청이 있으면 핵심 데모와 일정에 미치는 영향을 먼저 설명하고 명시적인 합의 없이 확장하지 않는다.

## 10. 구현 원칙

- 저장소의 실제 설정과 기존 패턴을 먼저 확인한다. 존재하지 않는 패키지 매니저, 프레임워크 버전, 명령 또는 인프라를 추정하지 않는다.
- 기획서의 기술 기준은 Next.js/Node.js 웹 프로젝트, PostgreSQL 테스트 DB, Playwright 검수다. 정확한 버전과 배포 방식은 저장소 설정이 정해질 때 기록한다.
- 작은 단위로 구현하고 제품 흐름을 끊지 않는 수직 슬라이스를 선호한다.
- 사용자 역할, 문서 버전, 승인 상태와 검수 상태를 문자열로 흩뿌리지 말고 중앙의 타입 또는 상수로 관리한다.
- 서버에서 권한과 상태 전이를 검증한다. UI에서 버튼을 숨기는 것만으로 권한을 보장하지 않는다.
- 외부 연동과 AI 출력은 실패, 지연, 재시도, 중복 이벤트와 부분 성공을 고려한다.
- GitHub webhook과 검수 요청은 멱등하게 처리하고 동일 이벤트의 중복 실행을 방지한다.
- 사용자 입력, AI 결과와 외부 로그를 신뢰하지 말고 검증 및 이스케이프한다.
- 비개발자용 요약과 전문가용 상세 로그를 계층적으로 제공한다.
- 접근성, 명확한 상태 표시, 로딩, 빈 상태, 오류와 재시도 경험을 기본 요구사항으로 본다.

## 11. 테스트와 완료 기준

변경 범위에 비례해 다음을 검증한다.

- 도메인 로직: 역할별 권한, 버전 고정, 승인과 상태 전이
- 핵심 흐름: 지원자 제출 및 비교, SOW 양측 승인, Commit SHA 검수, 수정 및 재검수, 최종 승인
- 경계 조건: 제한시간 만료, 중복 제출, 승인 후 변경, 오래된 커밋, 검수 시간 초과, 실패한 외부 연동
- 보안: 비인가 프로젝트 접근, 저장소 권한, 비밀정보 노출, 악성 로그 또는 입력
- UX: 비개발자가 기술 로그를 읽지 않고도 다음 행동을 이해할 수 있는지

기능 완료 시 관련 테스트, 타입 검사, 린트와 빌드를 실행한다. 실행 명령은 `package.json` 등 실제 프로젝트 설정에서 확인한다. 실행할 수 없는 검증은 이유와 남은 위험을 명시한다.

완료의 기준은 코드 작성 자체가 아니라 다음을 만족하는 것이다.

- 요구사항과 완료조건이 충족된다.
- 역할과 권한 경계가 지켜진다.
- 실패 및 재시도 경로가 설명 가능하다.
- 감사 가능한 근거와 이력이 남는다.
- 사용자 문구가 자동화를 전문가의 확정 판단처럼 과장하지 않는다.

## 12. 문서화와 커뮤니케이션

- 사용자용 문구와 프로젝트 문서는 기본적으로 한국어를 사용하되, 코드 식별자와 표준 기술 용어는 관례를 따른다.
- SOW, WBS, Acceptance Criteria, Definition of Done, PR, Commit SHA, Preview 등 핵심 용어를 일관되게 사용한다.
- 제품 동작, 데이터 구조, 보안 경계 또는 실행 절차를 변경하면 관련 문서를 함께 갱신한다.
- 구현 결과를 보고할 때 변경 내용, 검증 결과, 남은 제한을 구분해 설명한다.
- 기획과 충돌하거나 결정되지 않은 부분은 임의로 사실화하지 말고 가정 또는 제안으로 표시한다.

## 13. 제품 성공 지표

기능 판단과 실험 설계에서 다음 목표를 참고한다.

- 지원자 비교 시간: 기존 방식 대비 30% 단축
- 복수 평가자의 핵심 후보 순위 일치율: 70% 이상
- SOW 필수 항목 구체화율: 90% 이상
- 마일스톤 확인 시간: 기존 GitHub 직접 확인 대비 30% 단축
- 준비된 테스트 케이스 기준 자동 판정 정확도: 90% 이상
- 비개발 창업자 및 PO의 핵심 흐름 유용성: 평균 4.0/5 이상

제품의 최종 목적은 자동 판정 자체가 아니라 비개발 발주자가 비교, 합의, 검수와 승인에 필요한 기준과 증거를 확보하게 하는 것이다.
