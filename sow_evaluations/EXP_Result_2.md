# 제 2실험 결과 보고서: 파라미터 간 상관관계 및 영향도 분석 (EXP_Result_2.md)

## 📊 분석 개요
본 보고서는 `EXP_5_Example_Scenario.txt`의 104개 데이터셋과 물리적 API 호출 결과인 `benchmark_results.csv`를 교차 검증하여, **입력 파라미터(배경 유무, 요구사항 개수 등)가 업무 명세서 생성 결과(우선순위, 레이턴시, 번역 등)에 미치는 영향과 상관관계(Correlation)**를 분석한 자료입니다.

---

## 📈 1. 주요 파라미터 정의 및 통계적 상관관계 분석

데이터 분석 스크립트를 통해 도출된 핵심 변수들 간의 관계성입니다.

### 독립 변수 (입력 데이터)
1. **`has_background`**: [도입 배경 및 목적] 텍스트의 존재 여부 (0 또는 1)
2. **`feature_count`**: 핵심 요구사항(기능)의 물리적 개수 (4개 ~ 7개)
3. **`volume`**: 프로젝트 규모 명시 (소규모/중규모/대규모)

### 종속 변수 (결과 데이터)
1. **`llm_latency`**: OpenAI API 처리 응답 시간 (초 단위)
2. **`milestone_count`**: 최종적으로 도출된 마일스톤의 개수
3. **`budget_distribution`**: 예산 및 기간의 차등 분배 여부 (정성적 지표)

### 📊 통계 분석 요약 (상관계수 및 히트맵)

> 히트맵 이미지는 저장소에 포함되지 않아 아래 표의 수치로 결과를 기록합니다.

| 파라미터 조건 | 평균 응답 시간(Latency) | 평균 마일스톤 생성 개수 | 예산/일정 분배 양상 |
|---|---|---|---|
| **배경(Background) 없음** | `5.39초` | `4.96개` | 기능별 **단순 균등 분배** 성향 또는 **특정 기술 편향(할루시네이션)** |
| **배경(Background) 있음** | `5.57초` | `4.98개` | 배경 문제 해결을 위한 **핵심 인프라에 40% 이상 가중치 부여** |

> **분석 인사이트**:
> 배경 텍스트가 추가되어도 텍스트 처리량(토큰 수) 증가로 인한 응답 시간(Latency) 차이는 **불과 +0.18초**로 통계적으로 무의미했습니다. 하지만 **예산 분배 양상(정성적 결과)에는 절대적인 영향**을 미쳤습니다.

---

## 💡 2. 각 파라미터가 전체 과정에 미치는 정성적 영향도 (Deep-Dive)

한글 명세서 생성부터 영어 번역에 이르기까지, 각 파라미터가 AI에게 미치는 실제 영향력을 분석했습니다.

> 막대 그래프 이미지는 저장소에 포함되지 않아 아래 정성 분석으로 결과를 기록합니다.

### ① [도입 배경 및 목적] 유무가 미치는 영향 (영향력: ⭐️⭐️⭐️⭐️⭐️)
- **한글 마일스톤 생성 시**: 가장 결정적인 파라미터입니다. 배경이 주어지면 LLM은 **"무엇이 가장 시급한 비즈니스 문제인가?"**를 추론합니다. (예: 엑셀 수기 관리가 배경이면 -> '데이터베이스 구축' 마일스톤을 1순위로 격상). 반면 배경이 없으면 단순 기능 구현으로 인식하여 우선순위를 제대로 잡지 못합니다.
- **영어 명세서 번역 시**: 영어 번역의 **톤앤매너(Tone & Manner)**를 결정합니다. 배경이 B2B 엔터프라이즈면 번역 시 `Compliance`, `Audit` 같은 고급 비즈니스 어휘가 자연스럽게 혼합되지만, 배경이 없는 단순 MVP 앱이면 범용적인 개발 용어(CRUD 등)로만 번역됩니다.

### ② [요구사항 개수]가 미치는 영향 (영향력: ⭐️⭐️)
- **Rule-Base 모델**: 요구사항 개수(`feature_count`)와 도출되는 마일스톤 개수가 `상관계수 1.0`으로 완벽히 일치합니다. 7개를 주면 7개의 마일스톤을 1/7로 나눕니다.
- **LLM 모델**: 요구사항 개수와 마일스톤 개수 간의 **상관관계가 거의 없습니다(-0.06)**. LLM은 기능이 4개든 7개든 스스로 기능적 유사성을 그룹화(Grouping)하여, 사람이 관리하기 가장 좋은 **약 5개의 마일스톤**으로 알아서 통폐합하여 출력하는 뛰어난 능력을 보였습니다.

### ③ [프로젝트 규모(소/중/대)]가 미치는 영향 (영향력: ⭐️⭐️⭐️)
- **한글 마일스톤 생성 시**: '대규모(외부 API 다수 연동)'라는 키워드가 감지되면, LLM은 스스로 `QA 및 통합 테스트(Integration Test)`라는 명시되지 않은 마일스톤을 예산 15% 정도를 할당하여 마지막 순위로 추가하는 경향을 보였습니다. 규모 파라미터는 리스크 관리를 유도하는 핵심 트리거로 작동합니다.

---

## 🎯 3. 최종 결론 및 프롬프트 최적화 제안

이번 교차 분석을 통해 **"LLM에게 기능 목록만 던져주는 것은 Rule-Base와 다를 바 없는 결과(균등 분배)를 낳는다"**는 것을 수학적으로, 정성적으로 입증했습니다.

업무 명세서 생성 시스템의 품질을 극대화하려면 다음 사항이 시스템에 적용되어야 합니다:

1. **프론트엔드 입력 강제화**: 사용자가 명세서를 생성할 때, 단순 체크박스로 기능을 선택하게 하는 것에 그치지 않고 **"이 프로젝트를 왜 하려는지(배경/목적)"를 최소 50자 이상 필수 입력**하도록 UX를 설계해야 합니다.
2. **시스템 프롬프트(System Prompt) 개선**: 배경 지식이 누락되더라도 할루시네이션(예: 검색 엔진에 예산 60% 몰빵)이 발생하지 않도록, *"배경 정보가 불충분할 경우, 특정 기능에 예산의 30% 이상을 할당하지 마시오"* 라는 예산 통제 파라미터를 프롬프트 레벨에 하드코딩해야 합니다.
